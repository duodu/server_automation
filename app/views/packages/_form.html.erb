<%= form_for(@package) do |f| %>
  <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% @package.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b><%= f.label :ip %></b><br />
    <%= @package.ip %>
  </div>
  <div class="field">
    <b><%= f.label :username %></b><br />
    <%= @package.username %>
  </div>
  <div class="field">
    <b><%= f.label :sort %></b><br />
    <%= @package.sort %>
  </div>
  <div class="field">
    <b><%= f.label :package_array %></b><br />
    <%= @package.package_array %>
  </div>
  <div class="field">
    <b><%= f.label :path_id %></b><br />
    <%= f.select :path_id, options_for_select(@path) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
